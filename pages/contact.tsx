import Contact from "../components/contact";
import Layout from "../components/layout";
const contact = () => {
  return (
    <Layout>
      <Contact />
    </Layout>
  );
};

export default contact;
